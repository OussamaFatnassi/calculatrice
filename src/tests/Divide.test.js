import divide from "../components/Divide";

test("divide 10 and 2", () => {
    expect(divide(10, 2)).toEqual(5);

    }

);

test("divide 70 and 10", () => {
    expect(divide(70, 10)).toEqual(7);

    }

);

